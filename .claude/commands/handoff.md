End-of-session handoff. Do these in order:

1. Re-read `memory-bank/active-context.md`.

2. Rewrite `memory-bank/active-context.md` with:
   - "Last updated: <today's date>"
   - Current focus (1–2 lines)
   - What was decided this session (bullets, terse)
   - Next 1–3 concrete steps with enough detail for a fresh agent to pick up cold (file paths, branch names, exact commands where relevant)
   - Any open questions for the user
   - In-progress work: file paths, branches, partial commits, anything mid-flight

3. Append a dated entry to `memory-bank/progress.md` (newest at top, under the `---` divider) with what shipped this session in 3–6 bullets.

Be terse. No filler. No narrative. The user reads these to remember context — they don't need a summary, they need facts.

Confirm completion in one line: "Handoff written. Next session: `/north-star`."
